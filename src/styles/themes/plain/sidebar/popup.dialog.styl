@import '../../../mixins'

.Dialog .msg
  position: relative
  font-size: rem(16)
  padding: 0 8px
  color: var(--toolbar-fg)
  text-align: center

.Dialog .note
  position: relative
  font-size: rem(14)
  padding: 0 8px 8px
  color: var(--toolbar-fg)
  opacity: .7

.Dialog .ctrls .btn
  text-align: left
  min-width: auto
