@import '../../mixins'

.StyleField
  box(relative)
  padding: 2px 0
  margin: 0 12px 0 16px
  &:hover
    > .label
      color: var(--label-fg-hover)
  &.-no-top-margin
    margin-top: 0

.StyleField > .label
  box(relative)
  text(s: rem(15))
  color: var(--label-fg)
  transition: color var(--d-fast)

.StyleField > .input-group
  box(relative, flex)
  align-items: center
  margin: 6px 0 0

  > .text-input
    box(relative)
    size(100%)
    text(s: rem(15))
    color: var(--label-fg)
    margin: 0 16px 0 0
    transform: opacity var(--d-fast)

  > .toggle
    flex-shrink: 0
  
  &:not([data-active]) > .color-input
    opacity: .5